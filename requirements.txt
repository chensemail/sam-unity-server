flask
flask-cors

# CPU-only PyTorch (MUCH smaller than GPU version)
torch==2.2.0+cpu
torchvision==0.17.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

numpy
opencv-python-headless
Pillow
gdown

# Segment Anything + Gradio
segment-anything
gradio
